# Backend API Platform-DIEL (Fase 2)
fastapi>=0.110
uvicorn[standard]>=0.27
sqlalchemy>=2.0
pydantic>=2.6
email-validator>=2.0
python-multipart>=0.0.9
httpx>=0.27          # test client
pytest>=7.0

# Engine backtest/verify (paket lokal). Install editable:
#   pip install -e ../engine
# (numpy & pandas ikut dari sana)
